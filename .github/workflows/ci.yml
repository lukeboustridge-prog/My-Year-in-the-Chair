name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ✅ Install pnpm FIRST so it's on PATH
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false
          standalone: true

      # Then setup Node (no pnpm cache here to avoid calling pnpm too early)
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Verify pnpm
        run: pnpm -v

      # Optional cache for pnpm store (works with/without lockfile)
      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}-${{ hashFiles('package.json') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install deps
        run: pnpm install

      - name: Generate Prisma client
        run: pnpm prisma generate

      - name: Push Prisma schema (ephemeral SQLite)
        env:
          DATABASE_URL: "file:./dev.db"
        run: pnpm db:push

      - name: Build
        env:
          JWT_SECRET: "dev-only-secret"
          NEXT_TELEMETRY_DISABLED: "1"
        run: pnpm build
